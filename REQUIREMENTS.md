# 研究进度树（Research Progress Tree）— 需求规格 v1.0

> 状态：需求已与用户逐项确认（2025 会话内确认）。本文档是实现的唯一依据，变更需回到本文档修订。

## 1. 背景与目标

在 DSH 中进行长期探究（科研、调研、迁移分析、写作、代码探索等）时，过程本身难以回看：
做到哪一步了、探索过哪些分支、哪个分支成功/失败、为什么失败，缺乏一个实时的整体视图。

**目标**：在 DSH Web 界面（http://127.0.0.1:3080）侧边栏实时显示一棵"研究进度树"——
我们做了什么、做到哪里、哪个分支成功/失败（含原因），且跨会话持久留存。
定位不限于科研：任何需要长期探究的任务都适用。

## 2. 核心概念

### 2.1 一棵树 = 一个研究主题

- 树的根 = 一个研究主题（对应 DSH 的 goal；无 goal 时以会话主题为根）。
- 同一主题跨会话在**同一棵树**上累积。
- 不同主题各自成树，互不干扰。

### 2.2 先计划后对照

- 研究开始时，agent 先用工具画出**预期分支树**（方向A / 方向B / 验证步骤…）。
- 实际推进时实时对照：
  - 按计划走的 → 标记进度；
  - 偏离计划的 → 修正原节点或标记"修订"；
  - 计划外新增的 → 补充节点（标记"新增"）。

### 2.3 节点类型

| 类型 | 来源 | 说明 |
|---|---|---|
| `plan` | agent 计划阶段显式创建 | 预期分支 |
| `decision` | agent 研究中显式记录 | 探索分支的开启/结论（成功/失败 + 原因） |
| `subagent` | 自动（订阅事件） | 子代理生命周期节点 |
| `job` | 自动（订阅事件） | 后台任务节点 |
| `skill` | 自动（订阅事件） | 技能调用节点（可按主题聚合） |
| `goal-round` | 自动（订阅事件） | goal 轮次节点 |

### 2.4 状态与结论（混合判定）

- **自动状态**：`pending` / `running` / `ended`（由子代理、任务、goal 的真实状态映射）。
- **显式结论**：`success` / `failed` / `abandoned` / `blocked` + 原因文本，
  由 agent 在研究中记录，或用户手动标注。
- 两者并存：自动状态反映"机器事实"，显式结论反映"研究判断"（如：任务正常结束但研究上失败）。

### 2.5 当前指针

- 树上有且仅有一个"当前节点"，实时高亮，标识"我们现在做到哪里"。
- 自动跟随最新活动的节点。

## 3. 功能需求

### F1. 计划阶段（Phase 1）
- F1.1 agent 可创建计划树（根 + 多层子分支）。
- F1.2 计划节点可随时修正（改名、改父子、删除/标记废弃）。

### F2. 实时记录（Phase 1）
- F2.1 agent 可通过工具记录：开始一个分支、结束一个分支并给出结论与原因。
- F2.2 结论状态：`success` / `failed` / `abandoned` / `blocked`，必须可附带原因文本。
- F2.3 可关联产出文件（如 research-vault/ 下的产物）。

### F3. 自动骨架（Phase 1）
- F3.1 订阅 DSH 事件，自动生成 `subagent` / `job` / `skill` / `goal-round` 节点。
- F3.2 自动节点挂到当前活动分支之下。
- F3.3 噪音控制：自动节点默认折叠、可按类型聚合。

### F4. 侧边栏面板（Phase 1）
- F4.1 在 Web GUI 侧边栏渲染树：缩进层级、可折叠/展开。
- F4.2 状态用颜色区分（如：绿=成功、红=失败、灰=未开始、脉冲=进行中）。
- F4.3 当前节点高亮，并自动滚动到可视区域。
- F4.4 实时刷新（秒级），无需手动刷新页面。

### F5. 节点详情（Phase 1）
- F5.1 点击节点显示：标题、类型、自动状态、显式结论、原因、起止时间、关联文件、所属轮次。
- F5.2 关联文件可点击跳转（至少展示路径）。

### F6. 时间轴回放（Phase 2）
- F6.1 按时间轴回放树如何一步步长成（拖动滑块，树呈现历史快照）。

### F7. 从节点继续（Phase 3）
- F7.1 点击节点上的"从这继续"：生成一条给 agent 的消息（携带该节点上下文），
  agent 从该节点继续探究。

### F8. 持久化（Phase 1）
- F8.1 树数据持久化到工作区文件（`research-vault/<主题>/tree.json` 或等效位置）。
- F8.2 会话/进程重启后，同一主题的树完整恢复，可继续累积。
- F8.3 插件本身为动态插件（进程内），重启后需重新激活——后续评估注册为常驻插件
  （plugin inventory）以自动加载。

### F9. 通用性（贯穿）
- 不绑定具体科研技能；任何长期任务可独立使用。
- 与现有科研技能（deep-dive 等）的关系：技能是"干活的人"，本插件是"记录和展示的板子"；
  后续可让技能在调用时自动向树汇报（可选集成，非 Phase 1 范围）。

## 4. 非功能需求

- 实时性：节点状态变化在秒级反映到界面。
- 低噪音：自动节点可折叠/聚合，不淹没显式决策节点。
- 数据健壮性：tree.json 损坏时能降级（备份 + 报错，不丢历史）。
- 不侵入：插件停止/卸载后不影响 DSH 本体功能。

## 5. 数据模型（草案）

```jsonc
{
  "topic": "研究主题标题",
  "goalId": "goal-xxx | null",
  "createdAt": "ISO-8601",
  "updatedAt": "ISO-8601",
  "nodes": [
    {
      "id": "n1",
      "type": "plan | decision | subagent | job | skill | goal-round",
      "parentId": "n0 | null",
      "title": "方向A：尝试X方法",
      "status": "pending | running | ended",           // 自动状态
      "conclusion": "success | failed | abandoned | blocked | null",  // 显式结论
      "reason": "失败原因：… | null",
      "planRelation": "on-track | revised | added | null",  // 计划对照
      "files": ["research-vault/…"],
      "startedAt": "ISO-8601 | null",
      "endedAt": "ISO-8601 | null",
      "round": 3,
      "actor": "agent | user"
    }
  ],
  "currentNodeId": "n7",
  "timeline": [
    { "ts": "ISO-8601", "event": "node-started | node-concluded | plan-created | …", "nodeId": "n3" }
  ]
}
```

## 6. Agent 工具接口（草案）

`tree_node`（Host 端注册为动态工具，JSON 参数）：

```
tree_node({
  action: "plan-root" | "plan-child" | "start" | "conclude" | "annotate",
  parentId?,          // 父节点 id（plan-root 省略）
  title?,             // 节点标题
  status?,            // conclude 时: success | failed | abandoned | blocked
  reason?,            // 结论原因
  files?,             // 关联产出文件列表
  relation?           // plan 节点修正: on-track | revised | added
})
```

返回：新节点 id / 更新后的节点；错误时返回结构化错误（如 parentId 不存在）。

## 7. 技术方案

**DSH 动态插件（Cordis），Host + Client 双端**，理由见需求讨论记录：
- 能拿到 DSH 内部状态（goal、子代理、任务、技能事件）——独立网页做不到；
- 能在 Web GUI 的 Slot 中渲染原生界面——Markdown 日志做不到；
- 不改 DSH 源码，可独立升级/卸载。

```
┌─ Host 插件 ───────────────────────────────┐
│ · 动态工具 tree_node（F1/F2/F9）           │
│ · 订阅 DSH 事件 → 自动骨架（F3）           │
│ · 持久化 tree.json（F8）                   │
│ · 向 Client 提供树数据 RPC（F4）           │
└───────────────────────────────────────────┘
              ↓ 实时推送（事件/轮询）
┌─ Client 插件 ─────────────────────────────┐
│ 侧边栏树渲染（F4）· 详情抽屉（F5）          │
│ 时间轴回放（F6，Phase 2）                  │
│ "从这继续"按钮（F7，Phase 3）               │
└───────────────────────────────────────────┘
```

实现纪律（遵循 DSH 插件开发 SKILL）：
- 编码前用 `cordis_inspect_list` / `cordis_inspect_query` 确认真实接口
  （Slots 树、Event 列表、harness Builtins、timer 等），不臆造 API。
- 只读取所需叶子字段，不整体序列化内部对象。
- 所有副作用在插件生命周期内注册/清理。

## 8. 分期与验收

### Phase 1（MVP）：记录 + 骨架 + 侧边栏实时树 + 高亮 + 详情
验收：
1. agent 能通过 `tree_node` 创建计划树并记录分支结论（F1、F2）；
2. 子代理/后台任务/技能/轮次自动成节点，挂到当前分支下（F3）；
3. 侧边栏实时显示树，状态颜色正确，当前节点高亮且自动滚动（F4）；
4. 点击节点看详情（F5）；
5. 刷新页面、重启进程后，同一主题的树恢复并可继续累积（F8）。

### Phase 2：时间轴回放（F6）
### Phase 3：从节点继续（F7）+ 评估常驻插件化

## 9. 风险与已知问题

- 动态插件进程内生命周期：DSH 重启后需重新激活插件（Phase 3 评估常驻方案）。
- 事件覆盖度：某些内部事件（如 skill 调用）若无现成事件，用工具调用侧钩子或轮询兜底。
- 噪音控制是体验关键：自动节点过多时，需聚合/折叠策略（F3.3 必须落实，否则树不可读）。
